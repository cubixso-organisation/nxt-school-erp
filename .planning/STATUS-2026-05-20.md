# Local Setup Status — 2026-05-20

## Live

| Item | State |
|---|---|
| MySQL 9.6 (brew) | running |
| Database `nxtschools` | imported from `backup.sql` (232 MB → 433 MB) |
| Tables loaded | **150 / 150** (0 errors) |
| `.env` `DB_DSN` | `dbname=nxtschools` |
| Yii DB connectivity | verified |
| Aliased views | `pay_fees` → `payi_fees`, `payment_details` → `payment_detailis` |
| Node.js sibling | extracted to `/Users/gowthamreddy/Downloads/nxt_api_node/` (547 MB, 15 controllers, 58 Sequelize models) |
| Firebase JSONs | moved to `~/.config/nxtschools/`, repo root cleaned |
| AI audit tables | `ai_invocations` + `ai_proposals` created in `nxtschools` |
| AI scaffold | `components/ai/{AIClient,AuditLogger,PiiRedactor}.php` + `tools/{AbstractTool,ToolRegistry,FeeBalanceTool}.php` |
| `ai` Yii component | registered in `config/web.php` |
| `FeeBalanceTool` smoke | passed against real student #380 (pavan, class 10), admission lookup, not-found, full AIClient audit roundtrip |

## Real production data snapshot (unchanged)

```
student_details          12,586
student_class_attendance 367,258
payi_fees                22,966
payment_detailis         13,615
subject_timetable         7,513
exam_schedules            1,827
```

## Schema gotchas

| Yii model expects | Prod table | Fix in place |
|---|---|---|
| `pay_fees` | `payi_fees` | view `pay_fees` |
| `payment_details` | `payment_detailis` | view `payment_details` |
| `student_class.class_name` | `student_class.title` | `FeeBalanceTool` reads `title` directly |

## What was done this pass

1. Extracted `api.nxtschools.zip` → `/Users/gowthamreddy/Downloads/nxt_api_node/` (Express + Sequelize sibling, 547 MB)
2. Moved `nxtschool.json` + `estudent.json` to `~/.config/nxtschools/` (chmod 600), deleted from repo root
3. Patched `components/FirebaseNotification.php::getGoogleAccessToken()` to read `FIREBASE_NXTSCHOOL_KEY_PATH` env (fallback to `~/.config/nxtschools/nxtschool.json`)
4. Added `FIREBASE_*_KEY_PATH` + `ANTHROPIC_API_KEY` + `ANTHROPIC_MODEL` to `.env` and `.env.example`
5. Created `ai_invocations` (15 columns, 4 indexes) + `ai_proposals` (10 columns, 3 indexes)
6. Scaffolded `components/ai/`:
   - `AIClient.php` — Yii component, Anthropic Messages API wrapper, `callTool()` with audit
   - `AuditLogger.php` — `start()`, `finish()`, `proposal()` helpers
   - `PiiRedactor.php` — email/phone/Aadhaar scrubbing
   - `tools/AbstractTool.php` + `tools/ToolRegistry.php`
   - `tools/FeeBalanceTool.php` — student fee lookup over `payi_fees` + `payment_detailis`
7. Wired `'ai' => app\components\ai\AIClient` in `config/web.php`
8. Smoke-tested: 4 tests pass, audit row written to `ai_invocations`

## Still open (not blocking)

1. **Firebase JSONs in Node sibling**: `nxt_api_node/estudent.json` is a third copy. Same rotation plan applies, but Node sibling is its own codebase.
2. **Live secrets in `.env`**: SendGrid + Google Maps keys are real; rotate before any public share.
3. **Anthropic API key**: `.env` slot exists but is blank — needed before exercising `AIClient::chat()`. Tool calls work without it.
4. **Tool whitelist**: only `FeeBalanceTool` registered. Next tools to add for the fee chatbot pilot (B.2.7):
   - `StudentLookupTool` (resolve "pavan in 10A" → student_id)
   - `FeeStructureTool` (what does class X owe per term)
   - `PaymentHistoryTool` (paginated payment list with receipts)
5. **PHP 8.x migration**: not on the path — mpdf60 + tbs_class.php block it. PHP 7.4 stays.

## Next concrete step

Add the three companion tools above + a thin controller (e.g. `api/ChatbotController::actionAsk()`) that runs the Anthropic tool-use loop. Once `ANTHROPIC_API_KEY` is set, B.2.7 is live end-to-end.
